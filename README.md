## Kotlin Multiplatform demo project

### Running tests
To run tests on both platforms, execute the following command: `./gradlew allTests`

### Coverage report
Coverage report is available using Kover. Run `./gradlew koverHtmlReport` to get it. Remember that kover only covers JVM for now